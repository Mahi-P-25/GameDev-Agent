{
  "name": "@gamedev-agent/agent-runtime",
  "version": "0.1.0",
  "description": "Nova Agent Runtime — the multi-agent process manager. Agents are kernel-registerable, communicate through structured messages, and never depend on each other directly.",
  "type": "module",
  "license": "PROPRIETARY",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@gamedev-agent/di": "workspace:*",
    "@gamedev-agent/events": "workspace:*",
    "@gamedev-agent/kernel": "workspace:*",
    "@gamedev-agent/logging": "workspace:*",
    "@gamedev-agent/memory": "workspace:*",
    "@gamedev-agent/shared": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.3",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
