# Documentation

Architecture and operational documentation for GameDev Agent.

- `Workflow-Engine-Design.md` — goal-driven workflow engine (12 workflows).
- `Role-System-Design.md` — specialist AI role system (13 roles).
- `Studio-OS-Design.md` — studio operating system (layered architecture, 10-year view).

These documents are the source of truth for the system design and are referenced by the
implementation work in `packages/` and `apps/`.
