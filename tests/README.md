# Integration Tests

Cross-package integration tests for the GameDev Agent workspace. These tests validate the
foundation contracts and package boundaries rather than business logic.

Run with:

```bash
pnpm test
```

Or watch from the workspace root:

```bash
pnpm test:watch
```
