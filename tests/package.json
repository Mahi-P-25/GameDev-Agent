{
  "name": "@gamedev-agent/integration-tests",
  "version": "0.1.0",
  "description": "Cross-package integration tests for GameDev Agent.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo \"no build step\"",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "test": "vitest run"
  },
  "dependencies": {
    "@gamedev-agent/shared": "workspace:*",
    "@gamedev-agent/kernel": "workspace:*",
    "@gamedev-agent/di": "workspace:*",
    "@gamedev-agent/events": "workspace:*",
    "@gamedev-agent/tool-runtime": "workspace:*",
    "@gamedev-agent/workflow": "workspace:*",
    "@gamedev-agent/ami": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
