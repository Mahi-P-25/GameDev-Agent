# Scripts

Repository maintenance utilities, executed directly with Node (no build step required).

| Script         | Purpose                                                |
| -------------- | ------------------------------------------------------ |
| `clean.mjs`    | Removes `node_modules/.tmp`, `coverage`, and `.turbo`. |

Run with:

```bash
node scripts/clean.mjs
```
